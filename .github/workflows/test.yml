name: Test

on:
  push:
    paths-ignore:
      - "LICENSE"
    branches:
      - "main"
  pull_request_review:
    types: [submitted, edited]
  workflow_dispatch:

jobs:
  test:
    if: ${{ github.event_type == 'push' || github.event_type == 'workflow_dispatch' || github.event.review.state == 'approved' }}
    runs-on: 'ubuntu-20.04'
    steps:
      - run: echo 'Hello'

